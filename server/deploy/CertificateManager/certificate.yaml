apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: server-certificate
  namespace: wpsec
spec:
  secretName: server-tls-secret
  issuerRef:
    name: server-letsencrypt
    kind: Issuer
  dnsNames:
  - greything.com